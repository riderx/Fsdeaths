{
  "name": "fsdeaths",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "bun run build && wrangler deploy",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^6.1.8"
  },
  "devDependencies": {
    "wrangler": "^4.14.1"
  }
}
